# v0.8 — Lobby v2 + 观战页 v2

**Date**: 2026-05-19

## 技术栈升级

- **从 Babel standalone → Vite + React 19**：移除浏览器内编译，改为 build-time 编译
- 新子项目 `frontend-v2/`（`Vite 6 + React 19 + React Router 7`）
- Docker 多阶段构建（`node:20-alpine` build → `python:3.11-slim` serve）
- 部署于 `/v2/` 路径，`/lobby` 自动重定向至 `/v2/lobby`

## Lobby v2 完整重做

**五种槽位状态**:
- **空缺**：灰框，显示阵营名+君主名，可「配 AI 托管」或「加入」
- **AI 占用**：金框，显示 AI 标识，真人可「抢占」
- **已占用**：淡底，显示 IP 末段，「未就绪」标签
- **已就绪**：绿框，阵营色饱和，「已就绪」勾选
- **倒计时/对局中**：金框锁定，阵营+君主+agent 名

**新增功能**:
- 配 AI 托管：任意空缺位可一键配 AI，AI 自动就绪
- 抢位：倒计时未启动前，真人可抢占 AI 占位（AI 让出）
- 释放 AI：倒计时未启动前，可释放 AI 托管
- 5 秒倒计时全屏浮层
- 乐观更新：加入/配 AI 后立即更新本地 UI

## 观战页 v2 完整重做

**布局**（参考 FM 叙事中心 + Lichess）:
- 左栏 55%：SVG 7 城抽象地图 + Eval Bar 实力条 + 阵营信息卡 + 胜率曲线
- 右栏 45%：评书叙事流
- 底部：事件 feed（可展开战斗报告）+ Tick 进度条

**6 个借鉴范式**:
1. Lichess Eval Bar — 三色实力条
2. Lichess 事件 feed — 新事件高亮 0.5s + 自动滚动
3. Lichess 玩家卡 — 阵营信息卡（城/兵/粮）
4. Lichess SVG 走子箭头 — attack 进军可视化，2s 淡出
5. AlphaGo/Lichess 三色胜率曲线 — 前端累积 SVG path
6. FM 评书每 5 tick 增量生成 — DeepSeek V4 Flash LLM 生成

**新增**:
- SVG 7 城地图，城池归属颜色，闪烁攻占动画
- 三色实力条（蜀绿/魏蓝/吴红）
- 胜率曲线（前端累积每 tick 实力值）
- 事件流，点击展开完整战报（含大衍卦象）
- 评书增量叙事：每 5 tick 自动生成一段，LLM 生成或模板回退

## API 新增

| Endpoint | Method | Description |
|----------|--------|-------------|
| `/v1/lobby/assign-ai` | POST | 配 AI 托管到指定槽位 |
| `/v1/lobby/release-ai` | POST | 释放 AI 或玩家自己槽位 |
| `/v1/lobby/status` | GET | 新增 `chapters` 字段 |

**新增 error codes**:
- `COUNTDOWN_STARTED`：倒计时已启动时尝试加入

## 后端改动

- `app/narrator.py`：增量评书 narrator，DeepSeek V4 Flash 或模板回退
- `app/engine.py`：`current_game_state()` 新增 `chapters`、`factions.grain` 字段
- `app/engine.py`：`get_or_create_current_game()` 与 lobby 统一用 `is_active` 标记
- `app/lobby.py`：配 AI 托管/释放/抢位逻辑

## Bug 修复

- **v0.7**: 修复 `in_progress` 幽灵状态值（后端只用 `active`），前端集中判断 `isGameInProgress()`
- **v0.7**: 修复 `/current-game` 和 `/v1/lobby/status` 指向不同对局的 bug（`is_active` vs `is_current` 分歧）
- **v0.7**: 修复 spectate 页 `scrollIntoView` 导致全页面自动跳到底部的 bug
- **v0.7**: 修复事件流一闪而过——前端累积事件去重合并，不再依赖后端返完整历史
